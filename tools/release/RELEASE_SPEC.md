# Operit2 Release Specification

The release version, GitHub tag, update channel, and package asset rules are defined in:

```text
docs/release-versioning.md
```

`tools/release/release.py` must follow that document.
