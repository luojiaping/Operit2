name: operit2
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 2.0.0+6

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  crypto: ^3.0.6
  cupertino_icons: ^1.0.8
  flutter_math_fork:
    path: ../thirdparty/flutter_math_fork
  flutter_svg: ^2.3.0
  http: ^1.2.2
  web_socket_channel: ^3.0.3
  web: ^1.1.1
  dynamic_color: ^1.8.1
  hooks: ^2.0.0
  printing: ^5.14.3
  audioplayers: ^6.7.0
  video_player: ^2.11.1
  video_player_ohos:
    git:
      url: https://gitee.com/openharmony-sig/flutter_packages.git
      ref: 35fb467533e174411a117b2a030c15d2a3a9687c
      path: packages/video_player/video_player_ohos
  path_provider: ^2.1.5
  path_provider_ohos:
    git:
      url: https://gitee.com/openharmony-sig/flutter_packages.git
      ref: 35fb467533e174411a117b2a030c15d2a3a9687c
      path: packages/path_provider/path_provider_ohos
  path: ^1.9.1
  record: ^6.2.1
  record_ohos:
    path: ../thirdparty/record_ohos
  archive: ^4.0.9
  xml: ^6.6.1
  webview_all:
    path: ../thirdparty/webview_all
  webview_all_windows: ^1.2.1
  url_launcher: ^6.3.2
  url_launcher_ohos:
    git:
      url: https://gitee.com/openharmony-sig/flutter_packages.git
      ref: 35fb467533e174411a117b2a030c15d2a3a9687c
      path: packages/url_launcher/url_launcher_ohos
  file_selector: ^1.1.0
  file_selector_android:
    path: ../thirdparty/file_selector_android
  file_selector_platform_interface:
    path: ../thirdparty/file_selector_platform_interface
  file_selector_ohos:
    path: ../thirdparty/file_selector_ohos
  xterm:
    path: ../thirdparty/xterm
  liquid_glass_widgets:
    path: ../thirdparty/liquid_glass_widgets
  desktop_drop: ^0.7.1
  desktop_multi_window: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

dependency_overrides:
  file_selector_android:
    path: ../thirdparty/file_selector_android
  file_selector_platform_interface:
    path: ../thirdparty/file_selector_platform_interface
  webview_all_web:
    path: ../thirdparty/webview_all_web
  webview_all_windows:
    path: ../thirdparty/webview_all_windows
  webview_flutter_android:
    path: ../thirdparty/webview_flutter_android
  webview_flutter_platform_interface:
    path: ../thirdparty/webview_flutter_platform_interface

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
    - assets/images/operit_avatar.png
    - assets/model_logos/
    - path: assets/web_access/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/android/app/src/main/res/mipmap-xxxhdpi/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/assets/fonts/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/assets/images/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/fonts/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/packages/cupertino_icons/assets/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/packages/flutter_math_fork/lib/katex_fonts/fonts/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/packages/liquid_glass_widgets/shaders/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/assets/shaders/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/canvaskit/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/canvaskit/chromium/
      platforms: [android, ios, linux, macos, windows]
    - path: assets/web_access/icons/
      platforms: [android, ios, linux, macos, windows]
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: OperitTerminalMono
      fonts:
        - asset: assets/fonts/CascadiaMono.ttf

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
